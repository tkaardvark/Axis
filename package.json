{
  "name": "axis-analytics",
  "version": "1.0.0",
  "description": "NAIA Basketball Analytics",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd client && npm install && npm run build",
    "scrape": "node scrape-team-urls.js",
    "import": "node import-data.js",
    "analytics": "node calculate-analytics.js",
    "conferences": "node scrape-conferences.js",
    "setup-db": "node setup-database.js",
    "refresh": "npm run scrape && npm run import && npm run conferences && npm run analytics"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pg": "^8.17.2",
    "puppeteer": "^24.36.0"
  }
}
